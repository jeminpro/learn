Create a 100-question maths revision pack as a Word (.docx) document, formatted for an 11+ tutoring worksheet, covering these topics:

[Section 1 name]


[topic]
[topic]
[topic]


[Section 2 name]


[topic]
[topic]
[topic]


Follow these rules exactly:

Content


100 questions total, spread roughly evenly across all listed topics.
Within each topic, interleave difficulty (easy/medium/hard/challenge) rather than grouping questions by difficulty.
Do not show difficulty labels or hints on the question paper — keep it clean and minimal.
Number questions continuously 1–100 across the whole pack.
Use section headings matching the topic groups above.


Notation and formatting conventions


Never use the division symbol (÷) — express all division in fraction notation (e.g. 24/8, or a stacked fraction if rendering allows).
If any questions involve variables (e.g. equation rearranging), use capital letters only for variables, and bold the target variable.
BIDMAS-style questions use numbers only; rearrangement-style questions use variables only, always solving for X.
No answer lines or blanks directly under each question in the visible layout.


Working space


Leave space after each question for a pupil to write their working and answer.
Use three tiers of spacing:

Standard questions: a couple of lines of space.
Questions needing longer written answers (lists, worded problems): more space.
Written column methods (multiplication, division, large addition/subtraction): the most space.





Answers


Include a full answer key, but keep it separate from the questions — on its own page at the end of the document, not visible alongside each question.


Output


Deliver as a downloadable .docx file (not an inline HTML widget), using a monospace font for questions and answers, using the docx skill.
Verify formatting by rendering the document to PDF/images before sharing it.


To reuse this prompt: only edit the topic list above — everything else should stay the same.